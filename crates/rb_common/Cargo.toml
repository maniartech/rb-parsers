[package]
name = "rb_common"
version = "0.1.0"
edition = "2021"

[features]
# Enable serde Serialize/Deserialize derives on public types.
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }